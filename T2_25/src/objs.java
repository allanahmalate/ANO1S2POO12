/*
    import java.io.*;
    import java.text.*;
    import java.util.*;
*/

public class objs 
{
    /*private Massa array;
    private int cont;

    public objs()
    {
        array = new Massa[100];
        cont=0;
    }
    
     * Qualquer divisor funciona
     * A ordem de leitura de dados e a criterio individual
     * A ordem no 
     
    
     public int


      public int contarM()
      {
        int c=0;

        for(int i=0; i<cont; i++)
            //if(array[i].getMarca().substring(0,1).equals("M"))
            if(array[i].getMarca().substring(0,1).equals("M"))
                c++; //c+= array[i].getQTD();
      }
      */
}

